/**
 * Shared types for request body configuration.
 * Used by compose editor, collection editor, and session helpers.
 */
export type BodyType = "none" | "formdata" | "urlencoded" | "raw";
export type RawLanguage = "text" | "json" | "xml" | "html" | "javascript";
