# AIProxy v0.1.27

## 更新内容

- SSL 代理的包含/排除列表在条目较多时现在有固定高度和滚动条，不会把设置页无限拉长。

## What's new

- SSL Proxying Include and Exclude lists now have a fixed-height scrollable container, so a long list no longer stretches the settings page infinitely.
