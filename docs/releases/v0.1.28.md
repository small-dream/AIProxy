# AIProxy v0.1.28

## 更新内容

- 全量校对并更新应用内全部 15 篇双语使用指南，与当前版本功能对齐：规则匹配类型（Match Type）、多动作有序执行、批量启停/删除、拖拽排序、单文件规则导入导出；弱网模拟的 15 分钟临时启用与分块限速说明；断点自动放行与 JSON 守卫；会话搜索、多选与键盘导航；WebSocket 消息加载窗口与超时说明；Compose / Collections 的环境变量替换与文件附件用法等。

## What's new

- Fully reviewed and updated all 15 in-app bilingual user guides to match current behavior: rule Match Types, ordered multi-action execution, bulk management, drag-to-reorder, single-file rules import/export; Throttling's 15-minute temporary enable and chunked pacing; breakpoint auto-forward and the JSON guard; session search, multi-select and keyboard navigation; WebSocket load window and idle timeouts; Compose / Collections environment variables and file attachments.
